## Summary

Provide a brief description of the changes introduced by this PR.

## Type of Change

- [ ] Bug fix (non-breaking change which fixes an issue)
- [ ] New feature (non-breaking change which adds functionality)
- [ ] Breaking change (fix or feature that would cause existing functionality to not work as expected)
- [ ] Documentation update

## Verification Checklist

- [ ] `devtools::check()` / `R CMD check` passes locally with 0 errors.
- [ ] Frontend Svelte check passes (`npm run check` in `inst/codex_template`).
- [ ] Frontend unit tests pass (`npm run test` in `inst/codex_template`).
- [ ] Playwright E2E tests pass (`npm run test:e2e` in `inst/codex_template`).
- [ ] No personal or institutional affiliations have been committed.
